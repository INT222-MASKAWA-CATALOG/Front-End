version: "3"
services:
    frontend:
        image: ghcr.io/skeletonbear/front-end:latest
        container_name: frontend
        # ports: 
        #     - "8080:80"
        networks:
            - intregrated-network

networks:
    intregrated-network:
       external: true